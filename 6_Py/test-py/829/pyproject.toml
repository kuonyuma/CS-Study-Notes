[project]
name = "agent_demo"
version = "0.1.0"
description = "Add your description here"

requires-python = ">=3.14"
dependencies = [
    "aiomysql>=0.3.2",
    "google-genai>=2.19.0",
    "sqlalchemy>=2.0.52",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"